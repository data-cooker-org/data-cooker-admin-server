const { noteQuery } = require('./NoteQuery');
const { avatarQuery } = require('./AvatarQuery');
const { roleQuery } = require('./RoleQuery');
const { userQuery } = require('./UserQuery');
const { targetQuery } = require('./TargetQuery');
const { sourceQuery } = require('./SourceQuery');

module.exports = {
	noteQuery,
	avatarQuery,
	roleQuery,
	userQuery,
	targetQuery,
	sourceQuery,
};
